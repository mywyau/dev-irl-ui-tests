{
  "name": "dev-irl-ui-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:local": "BASE_URL=http://localhost:3000 playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "@types/node": "^24.0.1",
    "dotenv": "^16.5.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "consola": "^3.4.2"
  }
}
